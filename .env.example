# Optional server defaults for local/personal development.
ANTHROPIC_API_KEY=
SKETCHCAST_MODEL=claude-opus-4-8
DEEPGRAM_API_KEY=

# Required only for optional encrypted project sync.
# Provision with a private Vercel Blob store and pull with `vercel env pull`.
BLOB_READ_WRITE_TOKEN=
